CTF MODE ATTACKS — thinking like a CTF competitor:

1. JSON parser exploitation — extract_content() does manual byte parsing
2. Unicode NFC/NFD bypass — "é" vs "e+combining"
3. Race: normalize → write gap in execute_develop_action
4. Gateway auth bypass — protocol manipulation
5. Memory reading via /proc/self/mem
6. extract_content buffer overread
7. Session ID collision
8. env var injection
9. Capability check order exploitation
10. DeepSeek prompt exfiltration
